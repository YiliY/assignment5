...............
----------------------------------------------------------------------
Ran 15 tests in 4.472s

OK
callback=self.yesno_callback
		self.yesno_callback=None
	# if valid input for y and yes
		if words[0] in ('y','yes'):
			callback(True)
		
	#if valid input for n and no
		
		elif words[0] in ('n','no'):
			callback(False)
	# if input is invalid
